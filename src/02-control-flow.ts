function trimAndLower(text: string | null | undefined) {
  text;
  if (typeof text === "string") {
    text;
    return text.trim().toLocaleLowerCase();
  }
  text;
  return text;
}

function trimAndLower(text: string | null | undefined) {
  text;
  if (!text) {
    text;
    return text;
  }
  text;
  return text.trim().toLocaleLowerCase();
}

function trimAndLower(text: string | null | undefined) {
  return typeof text === "string" ? text.trim().toLocaleLowerCase() : text;
}


let foo: number | undefined
foo = 42
foo = undefined
console.log(foo)
